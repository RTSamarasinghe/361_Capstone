# For an actual database purpose, I would make an example .env file that would contain basic formatting that everyone can modify, and have everyone else use an uncommitted .env file on their own for security purposes of hiding the password.
# For purposes of ease involving this project, I will just put everything here.

# Database connection details
DB_SERVER=localhost\SQLEXPRESS
DB_NAME=ProjectDB
DB_TRUSTED_CONNECTION=true
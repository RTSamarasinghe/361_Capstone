export default function Checkout() {
  // shows the order, the price calculations
  // field for address info
  // field for billing info
  // button to submit order
  // return to home after order submit with notif at top of screen?
  return <div className=""></div>;
}
